#pragma once

#include "resource.h"

const static int DISPLAY_WIDTH = 800;
const static int DISPLAY_HEIGHT = 600;
const static int DISPLAY_WIDTH_HALF = DISPLAY_WIDTH / 2;
const static int DISPLAY_HEIGHT_HALF = DISPLAY_HEIGHT / 2;

